services:
  - type: web
    name: medical-report-agent
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python agent_app/app.py
    envVars:
      - key: HOST
        value: 0.0.0.0
